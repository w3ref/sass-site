<% fun_fact do %>
  Помните, что вы можете отключить предупреждения об устаревании от библиотек, которые вы не контролируете!
  Если вы используете интерфейс командной строки, вы можете передать [флаг `--quiet-deps`][`--quiet-deps` flag],
  а если вы используете JavaScript API, вы можете установить для [параметра `quietDeps`][`quietDeps` option] значение `true`.

  [`--quiet-deps` flag]: /documentation/cli/dart-sass#quiet-deps
  [`quietDeps` option]: /documentation/js-api/interfaces/StringOptionsWithoutImporter#quietDeps
<% end %>
